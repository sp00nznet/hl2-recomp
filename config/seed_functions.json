[
  {
    "start": "0x0059C950",
    "note": "Thread start routine observed at runtime; decodes as a function body."
  },
  {
    "start": "0x0059C59E",
    "note": "PsCreateSystemThreadEx StartContext1 observed at runtime; decodes as a function body."
  },
  {
    "start": "0x005ADC55",
    "note": "Indirect-call target observed at runtime; decodes as a function body."
  },
  {
    "start": "0x00620A53",
    "note": "Indirect-call target observed at runtime; decodes as a function body."
  },
  {
    "start": "0x0062264B",
    "note": "Indirect-call target observed at runtime; decodes as a function body."
  },
  {
    "start": "0x0001D0F8",
    "note": "Unresolved indirect-call target, 104k calls into the static-init walk. Sits in the gap between sub_0001CF86 and 0x0001D10D and decodes as a complete 21-byte comparator: push [edx]; push [ecx]; call 0x005B00E7; setl cl; ret."
  },
  {
    "start": "0x000618C1",
    "note": "Unresolved indirect-call target from the same run. In a gap, and decodes as a complete thiscall constructor returning this: mov esi, ecx; call 0x002A2711; and [esi+0xC], 0; mov eax, esi; ret."
  },
  {
    "start": "0x005AC780",
    "note": "Unresolved indirect-call target, observed by the ICALL feedback dump on the run that reaches module loading. Sits in the gap between sub_005AC750 and 0x005AC7B0 and decodes as a complete thiscall predicate: call 0x00581700; if true call 0x00581550; return 1. Its address is never an immediate -- it reaches a vtable slot only at runtime -- so no static pass can find it."
  },
  {
    "start": "0x005B21FB",
    "note": "Thread start routine, observed as PsCreateSystemThreadEx ctx1 on the run that completes static init. Lands inside sub_005B21F1 rather than at its start, so _pass_seed_aliases turns it into an alias; no static pass can find it, because the address only exists as an argument passed at runtime."
  },
  {
    "start": "0x0042F1A0",
    "note": "Unresolved indirect-call target on the run where D3D device creation first succeeds. Sits in the gap between sub_0042F190 and 0x0042F3A0, and decodes as a complete function: loads the interfaces at 0x009343D4 and 0x009353D4, saves esi/edi, then dispatches a run of virtual calls. Reached only through a vtable slot, so no static pass finds it."
  },
  {
    "start": "0x001F572B",
    "note": "Unresolved indirect-call target during level load. Starts with the __SEH_prolog idiom (push 0x18; push scopetable; call __SEH_prolog), which probes_as_prologue does not recognise, in a gap right after the previous function's ret. Skipping the call leaked its pushed args, which shifted the caller's frame and corrupted ebx."
  },
  {
    "start": "0x002CED77",
    "note": "Unresolved indirect-call target during level load. Starts with 'xor edx, edx' in a gap after a ret -- no prologue shape any static pass looks for."
  },
  {
    "start": "0x001B6366",
    "note": "Unresolved indirect-call target during level load. 'mov ecx, <imm32>; call <rel32>' -- a thiscall forwarding stub, in a gap after a ret. Deliberately seeded rather than matched by a rule: that shape occurs 13,359 times in this image and 12,464 of those are mid-function, so a pattern loose enough to catch this one is loose enough to split functions."
  }
]